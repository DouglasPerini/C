#include <stdio.h>

int main (){
    //Declarando variáveis 
    float valor_por_hora, salario;
    int horas_trabalhadas; 

    //Entradas
    printf("Informe valor recebido por hora;");
    scanf("%f", &valor_por_hora);

    printf("Informe horas trabalhadas por mês:");
    scanf()
}